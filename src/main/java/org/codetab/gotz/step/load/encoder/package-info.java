/**
 * Encoders which implements IEncoder, which encodes input Data type to other
 * types such as List<String>, List<DataSet> etc., and also may add delimiter
 * etc.,
 * @author Maithilish
 *
 */
package org.codetab.gotz.step.load.encoder;
